package com.dronebot.core.domain.control


//Actuator commands for the mixer/simulator: per‑motor outputs (normalized [0..1] or RPM), optional gimbal commands, beeper/leds
//Alternatively or additionally: setpoints produced by the autopilot (rate/attitude/altitude/position setpoints) if your mixer lives elsewhere.
//Metadata: timestamp and frame id.
//Purpose: a core type consumed by outbound/SimulatorOutputPort or any real actuator adapter.


class ControlOutput {

}
